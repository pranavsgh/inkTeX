#!/usr/bin/env bash
# Downloads the im2latex-100k dataset into backend/data/.
set -euo pipefail

DATA_DIR="$(dirname "$0")/../data"
mkdir -p "$DATA_DIR"

echo "TODO: download im2latex-100k into $DATA_DIR"
exit 1
